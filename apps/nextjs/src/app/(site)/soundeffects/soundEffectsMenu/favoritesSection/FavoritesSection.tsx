import type { SessionProps } from "../../types";

const FavoritesSection = ({ subData }: SessionProps) => {
  console.log("FavoritesSection", subData);

  return <div>Favorites Section</div>;
};

export default FavoritesSection;
